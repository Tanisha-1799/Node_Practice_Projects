module.exports={
    //making an object
    //since it is inside the object 
    //so we will not use "=" to assign values to variable
    //but we will use ":" to assign values to the variables
    x:10,
    y:20,
    z:function(){
        return "This is a function !!";
    }
};